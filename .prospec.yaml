version: "1.0"
project:
  name: cv-builder-multi-provider
paths:
  base_dir: prospec
exclude:
  - "*.env*"
  - "*credential*"
  - "*secret*"
  - node_modules
  - .git
agents:
  - claude
knowledge:
  base_path: prospec/ai-knowledge
